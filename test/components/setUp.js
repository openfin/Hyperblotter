// const jsdom = require('jsdom').jsdom;

// global.document = jsdom('');
// global.window = global.document.defaultView;

// Object.keys(global.window).map(key =>{
//     if(!global[key]){
//         global[key] = global.window[key];
//     }
// });

// global.navigator = 